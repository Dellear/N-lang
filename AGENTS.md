# AGENTS.md

## 前置说明
- 本文件用于规范本仓库的多 Agents 协作开发流程。
- 适用范围为仓库根目录及全部子目录；若子目录存在更近层级的 `AGENTS.md`，以更近文件为准。
- 本项目当前技术栈为 `TypeScript + Bun`，默认构建产物目录为 `dist/`。

## 目标
- 让多个 Agent 在同一仓库内并行工作时职责清晰、边界明确、结果可合并。
- 减少重复修改、隐式约定和相互覆盖。
- 保证所有结论、变更和验证结果可追溯。

## 全局规则
- 全部沟通、注释、文档、提交说明使用中文。
- 新文件统一使用 UTF-8 无 BOM 编码。
- 默认做最小闭环改动；若必须扩大范围，需要先在说明中写明原因。
- 默认允许破坏性替换，不主动保留旧接口、旧实现或过时代码；如无需迁移，明确写“无迁移，直接替换”。
- 严禁执行破坏性高风险命令，例如 `rm -rf`、无确认的大范围删除、覆盖未知文件。
- 不得上传密钥、令牌、内部地址或未脱敏数据到外部平台。
- 涉及公开资料检索时，优先官方文档与权威来源，并记录访问日期与用途。

## Agent 角色
### 1. Orchestrator
- 负责接收任务、拆解阶段、分配子任务、汇总状态。
- 负责维护任务计划、依赖关系、风险与最终交付说明。
- 不直接做大规模实现，除非没有其他 Agent 可接手。

### 2. Research Agent
- 负责阅读现有代码、文档、接口与依赖信息。
- 输出约束、假设、风险、影响面与证据链接。
- 未完成上下文梳理前，不给出最终实现结论。

### 3. Implement Agent
- 负责具体编码、重构、补全文档与必要注释。
- 保持单一主题修改，避免顺手修复无关问题。
- 修改前确认影响文件，修改后补充最基本验证。

### 4. Verify Agent
- 负责运行构建、测试、静态检查或最小可行验证。
- 记录命令、结果、失败现象与是否阻塞交付。
- 如果无法验证，必须说明原因、风险与建议下一步。

## 标准协作流程
### Phase 0：启动
- 明确目标、范围、成功标准、非目标。
- 建立计划，至少包含“调研、实现、验证、交付”四步。

### Phase 1：调研
- 先读 `README.md`、`package.json`、相关源码，再开始实现。
- 优先确认入口文件、构建方式、运行方式、现有约定。
- 输出最少包含：假设、影响范围、是否涉及迁移。

### Phase 2：实现
- 每个 Agent 只修改自己负责的文件集合。
- 如果多个 Agent 可能改同一文件，由 Orchestrator 先拆分为互不冲突的编辑区段。
- 新增逻辑必须配套更新中文文档或注释。

### Phase 3：验证
- 有新特性加入时，只添加新的测试用例，不修改已有的测试用例。
- 至少执行与改动直接相关的验证。
- 本项目默认验证优先级：
  1. `npm run build`
  2. 必要时用示例脚本做运行验证
- 验证失败时，先修复再交付；若受环境限制无法修复，需记录阻塞项。

### Phase 4：交付
- 交付内容必须包含：变更摘要、影响范围、验证结果、风险、迁移说明。
- 若无迁移动作，明确写“无迁移，直接替换”。

## 并行开发约束
- 禁止多个 Agent 在未协调的情况下同时修改同一段代码。
- 对公共文件的修改顺序为：接口定义先于调用方，底层能力先于上层接入。
- 若发现其他 Agent 已产生未合并改动，先读取并理解，再决定是否叠加，不得直接回退。
- 遇到冲突时优先保留最新、完整、可验证的实现，并补写冲突处理说明。

## 文件与目录约定
- `src/`：核心源码，所有功能改动默认在此目录完成。
- `dist/`：构建产物，不作为人工主编辑目标。
- `evidence/`：任务证据、决策记录、验证结果归档目录；不存在时可按需创建。

## 任务记录模板
每个任务开始或结束时，至少在 `evidence/` 留存一份简短记录，建议格式如下：

```md
# 任务记录
- 日期：YYYY-MM-DD
- 任务：一句话描述
- 负责人：Agent 名称
- 输入来源：用户请求 / 本地文件 / 官方文档
- 关键假设：...
- 修改文件：...
- 验证结果：...
- 风险与后续：...
```

## 交付模板
```md
前置说明：
- 假设与范围：...
- 数据时效：...

变更摘要：
- ...

验证结果：
- 已执行：...
- 未执行：...

迁移说明：
- 无迁移，直接替换
```

## 项目专属命令
- 安装依赖：`bun install`
- 开发运行：`bun run src/index.ts`
- NPM 启动：`npm start`
- 构建 JavaScript：`npm run build`
- 构建可执行文件：`npm run build:exe`

## 完成定义
- 代码可读、可构建、可追溯。
- 文档与实现一致。
- 验证结果明确，不把未验证内容描述成已完成。
- 交付说明中包含迁移结论；本项目默认写法为“无迁移，直接替换”。

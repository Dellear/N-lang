# 任务记录
- 日期：2026-03-13
- 任务：为 N-lang 新增 Date 内置对象支持，并将测试并入现有 test.n
- 负责人：Codex
- 输入来源：用户请求、`src/interpreter.ts`、`README.md`、`test.n`
- 关键假设：首版 Date 支持不新增语法，仅通过解释器运行时内置对象接入
- 修改文件：`src/interpreter.ts`、`README.md`、`test.n`
- 验证结果：`npm run build` 通过；`bun run src/index.ts test.n` 通过；Date 构造、属性、方法、比较和差值行为符合冻结接口
- 风险与后续：当前 Date 仍依赖宿主 JavaScript Date 的解析与本地时区行为；若后续需要 UTC 构造、格式模板或时区转换，建议二期单独扩展

# 任务记录
- 日期：2026-03-13
- 任务：纠正依赖安装方式，统一回到 Bun 包管理
- 负责人：Codex
- 输入来源：用户请求、`package.json`
- 关键假设：项目以 Bun 为唯一依赖管理工具，npm 产生的 lockfile 不应保留
- 修改文件：`bun.lockb`
- 验证结果：已删除 `package-lock.json`，已执行 `bun install`，已通过 `bun run build`
- 风险与后续：无；后续新增依赖统一使用 `bun add` 或 `bun install`

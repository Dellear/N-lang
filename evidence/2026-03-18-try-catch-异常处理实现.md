# 任务记录
- 日期：2026-03-18
- 任务：实现 try/catch/finally + throw 异常处理机制
- 负责人：Implement Agent
- 输入来源：用户请求
- 关键假设：throw 可抛出任意 Value；ReturnSignal/BreakSignal/ContinueSignal 不被 catch 拦截，正常穿透
- 修改文件：`src/lexer.ts`、`src/ast.ts`、`src/parser.ts`、`src/interpreter.ts`、`test.n`
- 验证结果：test.n 全量通过（含新增第 22 节异常处理测试用例）
- 风险与后续：无

---

前置说明：
- 假设与范围：新增 try/catch/finally/throw 四个关键字，AST 新增 Throw 和 Try 节点，interpreter 新增 ThrowSignal 信号类
- 数据时效：2026-03-18

变更摘要：
- `src/lexer.ts`：TT 枚举新增 TRY/CATCH/FINALLY/THROW；KW 表新增对应映射
- `src/ast.ts`：新增 Throw、Try 节点类型
- `src/parser.ts`：parseStmt 新增 throw 和 try 解析分支；try 要求至少有 catch 或 finally
- `src/interpreter.ts`：新增 ThrowSignal 类；exec 新增 Throw（包装为 ThrowSignal）和 Try（区分控制流信号与异常信号，finally 始终执行）
- `test.n`：新增第 22 节测试（基础 throw/catch、finally、运行时错误捕获、嵌套、re-throw、不同类型值、函数异常、return/break/continue 穿透）

验证结果：
- 已执行：test.n 全量回归通过（21 节旧测试 + 第 22 节异常处理）
- 未执行：无

迁移说明：
- 无迁移，直接替换

# 任务记录
- 日期：2026-03-18
- 任务：实现 class 语法支持（含单继承、多层继承）
- 负责人：Implement Agent
- 输入来源：用户请求
- 关键假设：this 作为普通环境变量绑定；super() 通过 `__class__` 隐藏变量沿继承链查找父类构造函数
- 修改文件：`src/lexer.ts`、`src/ast.ts`、`src/parser.ts`、`src/interpreter.ts`、`test.n`
- 验证结果：test.n 全量通过（含新增第 19-21 节 class 测试用例）
- 风险与后续：lexer KW 映射改用 `Object.create(null)` 修复了 `constructor` 原型污染问题

---

前置说明：
- 假设与范围：新增 class/extends/new/super/this 五个关键字，AST 新增 Class 和 New 节点，interpreter 新增 NClass/NInstance 类型及继承链方法查找
- 数据时效：2026-03-18

变更摘要：
- `src/lexer.ts`：新增 5 个关键字；KW 映射改用 `Object.create(null)` 避免原型污染
- `src/ast.ts`：新增 Class、New 节点类型
- `src/parser.ts`：parseStmt 增加 class 解析；parsePrimary 增加 new/this/super 分支
- `src/interpreter.ts`：新增 NClass/NInstance 类型、`findMethod()` 继承链查找、Class/New 执行逻辑、super() 处理、实例属性读写和方法调用
- `test.n`：新增第 19-21 节测试（基础类、单继承、多层继承）

验证结果：
- 已执行：test.n 全量回归通过（含 class 基础、继承、多层继承、typeof 检查）
- 未执行：无

迁移说明：
- 无迁移，直接替换
